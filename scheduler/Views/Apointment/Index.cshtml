@model IEnumerable<scheduler.Models.ApointmentModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ApointmentName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ApointmentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ApointmentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ApointmentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ApointmentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
